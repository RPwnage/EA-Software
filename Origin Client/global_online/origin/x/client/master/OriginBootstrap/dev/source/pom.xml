<?xml version="1.0" encoding="UTF-8" standalone="no"?><project>
	<modelVersion>4.0.0</modelVersion>

	<groupId>com.ea.link</groupId>
	<artifactId>originBootstrap</artifactId>
	<name>originBootstrap</name>
	<version>${version.name}</version>
	<packaging>pom</packaging>
	
	<parent>
		<groupId>com.ea.core</groupId>
		<artifactId>MavenVSParent</artifactId>
		<version>1.1.0-SNAPSHOT</version>
	</parent>

  <!-- this is the magical module that generates the version.h file  -->
  <modules>
    <module>..\version</module>
  </modules>

  <!-- Source Control Management -->
	<!--
		http://maven.apache.org/ref/current/maven-model/maven.html#class_scm
	-->
	<!--
		NOTE: This block must exist independently in each child project!
	-->
	
	<distributionManagement>
		<!-- Fusion Snapshots -->
		<snapshotRepository>
			<id>fusion.snapshots</id>
			<url>http://ebisu-build03:8088/nexus/content/repositories/snapshots</url>
			<uniqueVersion>false</uniqueVersion>
		</snapshotRepository>
	</distributionManagement>

	<properties>
		<resourceDir>mavenResources</resourceDir>
		<build.configuration>release</build.configuration>
		<sln.filename>OriginBootstrap.sln</sln.filename>
		<branch.name>8.6</branch.name>
		<version.name>8.6.0-SNAPSHOT</version.name>
	</properties>

	<scm>
		<connection>
            scm:perforce:p4serv1.online.ea.com:4487://ebisu/client/$(branch.name)/${artifactId}
        </connection>
		<developerConnection>${scm.connection}</developerConnection>
		<url>http://docs.online.ea.com:4487/ebisu/client/$(branch.name)/${artifactId}</url>
	</scm>
	
	
	
	<build>
		<resources>
			<resource>
				<!--  has to be a better way to access resource dir -->
				<directory>${resourceDir}</directory>
				<filtering>true</filtering>
			</resource>
		</resources>
		<plugins>
			<plugin>
				<artifactId>maven-clean-plugin</artifactId>
				<version>2.4</version>
				<configuration>
					<filesets>
						<fileset>
							<directory>${basedir}/${build.configuration}</directory>
						</fileset>
					</filesets>
				</configuration>
			</plugin>
			
			<!-- Zip up the relevant build artifacts -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-assembly-plugin</artifactId>
				<executions>
					<execution>
						<id>release-build</id>
						<phase>package</phase>
						<goals>
							<goal>single</goal>
						</goals>
						<configuration>
							<finalName>${artifactId}</finalName>
							<descriptors>
								<descriptor>${artifactId}-assembly.xml</descriptor>
							</descriptors>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
	
	<dependencies>
	</dependencies>
</project>
