<project>

	<!-- UpdateTool public header files location. -->
	<property name="package.UpdateTool.includedirs">
		${package.UpdateTool.dir}/include
	</property>

	<!-- UpdateTool binary files location. -->
	<property name="package.UpdateTool.bindir" value="${package.UpdateTool.dir}/bin" />

	<!-- UpdateTool source files location. -->
	<property name="package.UpdateTool.sourcedir" value="${package.UpdateTool.dir}/source" />
</project>
