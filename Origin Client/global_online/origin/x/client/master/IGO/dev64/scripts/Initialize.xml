<project>

	<!-- IGO public header files location. -->
	<property name="package.IGO.includedirs">
		${package.IGO.dir}/..
		${package.IGO.dir}/../../IGOLIB
	</property>
    <property name="package.IGO.lib" value="${package.IGO.dir}/../lib" />
    <property name="package.IGO.bin" value="${package.IGO.dir}/../bin" />

</project>
