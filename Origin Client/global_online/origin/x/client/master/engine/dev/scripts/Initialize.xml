<project>
	<property name="package.engine.includedirs">
		${package.engine.dir}/include
	</property>

	<!-- EAStdC libraries location. -->
	<fileset name="package.engine.libs">
		<includes name="${package.engine.builddir}/${config}/lib/${lib-prefix}engine${lib-suffix}" />
	</fileset>
</project>