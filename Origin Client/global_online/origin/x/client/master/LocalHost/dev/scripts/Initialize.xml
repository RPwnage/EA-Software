<project>
	<property name="package.LocalHost.includedirs">
		${package.LocalHost.dir}/include
	</property>

	<!-- LocalHost libraries location. -->
	<fileset name="package.LocalHost.libs">
		<includes name="${package.LocalHost.builddir}/${config}/lib/${lib-prefix}LocalHost${lib-suffix}" />
	</fileset>
</project>