<project>
	<property name="package.sdkService.includedirs">
		${package.sdkService.dir}/include
	</property>

	<!-- EAStdC libraries location. -->
	<fileset name="package.sdkService.libs">
		<includes name="${package.sdkService.builddir}/${config}/lib/${lib-prefix}sdkService${lib-suffix}" />
	</fileset>
</project>