#include "Project.xcconfig"

GCC_PREPROCESSOR_DEFINITIONS =		// Prefix your DEBUG macro to avoid collisions.
ONLY_ACTIVE_ARCH = NO               // Don't build universal.
GCC_OPTIMIZATION_LEVEL = 0			// Don't optimize.
COPY_PHASE_STRIP = NO				// Don't strip if we install (we don't usually).
GCC_DYNAMIC_NO_PIC = NO;
GCC_ENABLE_OBJC_EXCEPTIONS = YES;
CLANG_WARN_CONSTANT_CONVERSION = YES;
CLANG_WARN_ENUM_CONVERSION = YES;
CLANG_WARN_INT_CONVERSION = YES;
CLANG_WARN__DUPLICATE_METHOD_MATCH = YES;
GCC_WARN_ABOUT_RETURN_TYPE = YES;
GCC_WARN_UNINITIALIZED_AUTOS = YES;
GCC_WARN_UNUSED_VARIABLE = YES;
COMBINE_HIDPI_IMAGES = YES;
