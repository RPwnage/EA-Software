<project>

	<!-- TelemetryBreakpointHandler public header files location. -->
	<property name="package.TelemetryBreakpointHandler.includedirs">
		${package.TelemetryBreakpointHandler.dir}/include
	</property>

	<!-- TelemetryBreakpointHandler binary files location. -->
	<property name="package.TelemetryBreakpointHandler.bindir" value="${package.TelemetryBreakpointHandler.dir}/bin" />

	<!-- TelemetryBreakpointHandler source files location. -->
	<property name="package.TelemetryBreakpointHandler.sourcedir" value="${package.TelemetryBreakpointHandler.dir}/source" />

</project>
