<project>

	<!-- IGOProxy public header files location. -->
	<property name="package.IGOProxy.includedirs">
		${package.IGOProxy.dir}/..
	</property>

	<!-- IGOProxy binary files location. -->
	<property name="package.IGOProxy.bindir" value="${package.IGOProxy.dir}/../bin" />

	<!-- IGOProxy source files location. -->
	<property name="package.IGOProxy.sourcedir" value="${package.IGOProxy.dir}/.." />

</project>
