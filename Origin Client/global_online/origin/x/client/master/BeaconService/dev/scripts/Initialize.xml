<project>
	<property name="package.BeaconService.includedirs">
		${package.BeaconService.dir}/include
	</property>
	
	<property name="package.BeaconService.bindir" value="${package.BeaconService.builddir}/${config}/bin" />
	
	<property name="package.BeaconService.libdir" value="${package.BeaconService.builddir}/${config}/lib/${lib-prefix}BeaconService${lib-suffix}" />
	
	
</project>