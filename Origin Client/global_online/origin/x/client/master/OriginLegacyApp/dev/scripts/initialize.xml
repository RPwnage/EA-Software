<project>

  <!--
    Dependent packages may use ${package.OriginApp.includedirs} in their 
    includedirs element of build tasks.
  -->
  <property name="package.OriginLegacyApp.includedirs">
    ${package.OriginLegacyApp.dir}/include
    ${package.OriginLegacyApp.dir}/source
    ${package.OriginLegacyApp.dir}/source/ui
    ${package.OriginLegacyApp.dir}/source/ui/Middleware
    ${package.OriginLegacyApp.dir}/source/EbisuCommonUI
    ${package.OriginLegacyApp.dir}/source/EbisuCommonUI/EbisuCommonControls
    ${package.OriginLegacyApp.dir}/source/EbisuCommon
    ${package.OriginLegacyApp.dir}/source/OriginEngine/include
    ${package.OriginLegacyApp.dir}/source/Middleware
    ${package.OriginLegacyApp.dir}/source/CommonQt
    ${package.OriginLegacyApp.dir}/source/IGO/IGOLIB
    ${package.OriginLegacyApp.dir}/generated/${config}/
  </property>

  <!--
    Dependent packages may use ${package.OriginLegacyApp.libs} in their 
    libs element of build tasks.
  -->
  <property name="package.OriginLegacyApp.libdir" value="${package.OriginLegacyApp.dir}/lib" />

  <property name="package.OriginLegacyApp.bin" value="${package.OriginLegacyApp.dir}/bin" />

  <property name="package.OriginLegacyApp.sourcedir" value="${package.OriginLegacyApp.dir}/source" />

    <fileset name="package.OriginLegacyApp.libs">
        <includes name="${package.OriginLegacyApp.libdir}/${lib-prefix}OriginLegacyApp${debug-suffix}${lib-suffix}" />
    </fileset>

</project>
