<?xml version="1.0" encoding="UTF-8" standalone="no"?><project>
	<modelVersion>4.0.0</modelVersion>

	<groupId>com.ea.link</groupId>
	<artifactId>OriginRuntime</artifactId>
	<version>${version.name}</version>
	<name>Origin Client Distribution</name>
	<packaging>pom</packaging>
	
	<distributionManagement>
		<!-- Fusion Snapshots -->
		<snapshotRepository>
			<id>fusion.snapshots</id>
			<url>http://ebisu-build03:8088/nexus/content/repositories/snapshots</url>
			<uniqueVersion>false</uniqueVersion>
		</snapshotRepository>
	</distributionManagement>

	<properties>
		<resourceDir>mavenResources</resourceDir>
		<build.configuration>release</build.configuration>
		<sln.filename>${artifactId}.sln</sln.filename>
		<branch.name>8.6</branch.name>
		<version.name>8.6.0-SNAPSHOT</version.name>
	</properties>

	<scm>
		<connection>
            scm:perforce:p4serv1.online.ea.com:4487://ebisu/client/$(branch.name)/${artifactId}
        </connection>
		<developerConnection>${scm.connection}</developerConnection>
		<url>http://docs.online.ea.com:4487/ebisu/client/$(branch.name)/${artifactId}</url>
	</scm>
	
	<build>
		<resources>
			<resource>
				<!--  has to be a better way to access resource dir -->
				<directory>${resourceDir}</directory>
				<filtering>true</filtering>
			</resource>
		</resources>
		<plugins>
		  <plugin>
			<groupId>org.apache.maven.plugins</groupId>
			<artifactId>maven-dependency-plugin</artifactId>
			<executions>
			  <execution>
				<id>copy</id>
				<phase>package</phase>
				<goals>
				  <goal>unpack</goal>
				</goals>
				<configuration>
				  <artifactItems>
					<artifactItem>
					  <groupId>com.ea.link</groupId>
					  <artifactId>origin</artifactId>
					  <version>${version.name}</version>
					  <overWrite>true</overWrite>
					  <outputDirectory>target/temp</outputDirectory>
					  <type>zip</type>
					</artifactItem>

					<artifactItem>
					  <groupId>com.ea.link</groupId>
					  <artifactId>EscalationService</artifactId>
					  <version>${version.name}</version>
					  <overWrite>true</overWrite>
					  <outputDirectory>target/temp</outputDirectory>
					  <type>zip</type>
					</artifactItem>
            
          <artifactItem>
              <groupId>com.ea.link</groupId>
              <artifactId>EACoreCLI</artifactId>
              <version>${version.name}</version>
              <overWrite>true</overWrite>
              <outputDirectory>target/temp</outputDirectory>
              <type>zip</type>
            </artifactItem>
          </artifactItems>
				  
				  <!-- other configurations here -->
				</configuration>
			  </execution>
			</executions>
		  </plugin>
		  <plugin>
			<artifactId>maven-clean-plugin</artifactId>
			<version>2.4</version>
			<configuration>
				<filesets>
					<fileset>
						<directory>${basedir}/${build.configuration}</directory>
					</fileset>
				</filesets>
			</configuration>
			</plugin>
			
			<!-- Zip up the relevant build artifacts -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-assembly-plugin</artifactId>
				<executions>
					<execution>
						<id>release-build</id>
						<phase>package</phase>
						<goals>
							<goal>single</goal>
						</goals>
						<configuration>
							<finalName>${artifactId}</finalName>
							<descriptors>
								<descriptor>${artifactId}-assembly.xml</descriptor>
							</descriptors>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
	
	
	<dependencies>
		<dependency>
			<groupId>com.ea.link</groupId>
			<artifactId>origin</artifactId>
			<version>${version.name}</version>
			<type>zip</type>
		</dependency>
		
		<dependency>
			<groupId>com.ea.link</groupId>
			<artifactId>EscalationService</artifactId>
			<version>${version.name}</version>
			<type>zip</type>
		</dependency>

    <dependency>
      <groupId>com.ea.link</groupId>
      <artifactId>EACoreCLI</artifactId>
      <version>${version.name}</version>
      <type>zip</type>
    </dependency>
  </dependencies>
</project>
