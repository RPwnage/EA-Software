<project>
	<property name="package.OriginER.includedirs">
		${package.OriginER.dir}/include
	</property>

	<!-- EAStdC libraries location. -->
	<fileset name="package.OriginER.libs">
		<includes name="${package.OriginER.builddir}/${config}/lib/${lib-prefix}OriginER${lib-suffix}" />
	</fileset>
</project>