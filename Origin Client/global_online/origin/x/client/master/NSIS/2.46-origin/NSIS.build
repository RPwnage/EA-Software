<project name="NSIS" default="build">
	
    <!-- ******************* -->
    <!-- Package declaration -->
    <!-- ******************* -->
    <property name="package.NSIS.dir"  value="@{PathGetFullPath('.')}"/>
    <property name="package.NSIS.version"  value="@{PathGetFileName('${package.NSIS.dir}')}"/>
    <package name="NSIS" targetversion="${package.NSIS.version}"/>

</project>




