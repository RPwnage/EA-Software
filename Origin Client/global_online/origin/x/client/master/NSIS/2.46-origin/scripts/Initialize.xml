<project>
	<!-- NSIS binary files location. -->
	<property name="package.NSIS.bindir" value="${package.NSIS.dir}/dist/bin" />

	<!-- NSIS source files location. -->
	<property name="package.NSIS.sourcedir" value="${package.NSIS.dir}/source" />

	<!-- NSIS executable -->
	<property name="package.NSIS.exe" value="${package.NSIS.dir}/dist/Unicode/makensis.exe" />

	<!-- NSIS executable distribution dir -->
	<property name="package.NSIS.distdir" value="${package.NSIS.dir}/dist" />

</project>
