<project>

	<!-- TelemetryAPI header files location. -->
	<property name="package.OriginVersion.includedirs">
		${package.OriginVersion.dir}/include
	</property>

	<property name="package.OriginVersion.targetdir" value="${package.OriginVersion.dir}/target" />
 	<property name="package.OriginVersion.version.file" value="${package.OriginVersion.targetdir}/version.txt" />
 	<property name="package.OriginVersion.plugin.version.file" value="${package.OriginVersion.targetdir}/pluginversion.txt" />

</project>
