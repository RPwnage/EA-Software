<project>
	<property name="package.services.includedirs">
		${package.services.dir}/include
	</property>

	<!-- EAStdC libraries location. -->
	<fileset name="package.services.libs">
		<includes name="${package.services.builddir}/${config}/lib/${lib-prefix}services${lib-suffix}" />
	</fileset>
</project>