<project>

	<!-- SonarVoiceServer public header files location. -->
	<property name="package.SonarVoiceServer.includedirs">
	</property>

	<!-- SonarVoiceServer binary files location. -->
	<property name="package.SonarVoiceServer.bindir" value="${package.SonarVoiceServer.dir}/bin" />

	<!-- SonarVoiceServer source files location. -->
	<property name="package.SonarVoiceServer.sourcedir" value="${package.SonarVoiceServer.dir}" />

</project>
