<project default="package">
    <!-- ******************* -->
    <!-- Package declaration -->
    <!-- ******************* -->
    <property name="package.OriginConfig.dir"  value="@{PathGetFullPath('.')}"/>
    <property name="package.OriginConfig.version"  value="@{PathGetFileName('${package.OriginConfig.dir}')}"/>
    <package name="OriginConfig" targetversion="${package.OriginConfig.version}"/>
</project>
