	OriginErrorT OriginIGORenderer::beginFrame()
	{
		// start capturing the next frame
		if (m_streamingProperties.streaming
			&& (m_streamingProperties.frameTimeStamp - EA::StdC::GetTime()) >= (1000000.0 / m_streamingProperties.fps))
			{
				// Capture a frame form streaming.
			}

		// Start your custom drawing for this frame.
		
		return ORIGIN_SUCCESS_IGOAPI;
	}