OriginErrorT ticketCallback(void * pContext, const OriginCharT **ticket, int32_t length, OriginErrorT err);

void exampleAsynchronousCall()
{
   OriginErrorT err = OriginRequestTicket(OriginGetDefaultUser(), ticketCallback, &g_TicketReceived);
   if (err == ORIGIN_SUCCESS) printf("Success\n");
}

OriginErrorT ticketCallback(void * pContext, const OriginCharT **ticket, int32_t length, OriginErrorT err)
{
   if(ticket != 0 && *ticket != 0)
       printf("Async: %S", *ticket);
   else
       printf("Async: No Ticket Available.");

   return ORIGIN_SUCCESS;
}