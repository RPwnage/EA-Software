// Callback for All SDK Event handling
OriginErrorT EventCallback (int32_t eventId, void* pContext, void* eventData, uint32_t count)
{
   return ORIGIN_SUCCESS;
}

// Callback for SDK IGO Event handling
OriginErrorT IgoEventCallback (int32_t eventId, void* pContext, void* eventData, uint32_t count)
{
   uint32_t state = *(uint32_t *)eventData;
   return ORIGIN_SUCCESS;
}

// Callback for SDK Invite Event handling
OriginErrorT InviteEventCallback (int32_t eventId, void* pContext, void* eventData, uint32_t count)
{
   OriginInviteT *invite = (OriginInviteT *)eventData;
   return ORIGIN_SUCCESS;
}

// Callback for SDK Login Event handling
OriginErrorT LoginEventCallback (int32_t eventId, void* pContext, void* eventData, uint32_t count)
{
   uint32_t *event = (uint32_t *)eventData;
   return ORIGIN_SUCCESS;
}

// Callback for SDK Profile Event handling
OriginErrorT ProfileEventCallback (int32_t eventId, void* pContext, void* eventData, uint32_t count)
{
   uint32_t *event = (uint32_t *)eventData;
   return ORIGIN_SUCCESS;
}

// Callback for SDK Presence Event handling
OriginErrorT PresenceEventCallback (int32_t eventId, void* pContext, void* eventData, uint32_t count)
{
   OriginPresenceT *event = (OriginPresenceT *)eventData;
   return ORIGIN_SUCCESS;
}

// Callback for SDK Friends Event handling
OriginErrorT FriendsEventCallback (int32_t eventId, void* pContext, void* eventData, uint32_t count)
{
   OriginFriendT *friends = (OriginFriendT *)eventData;
   return ORIGIN_SUCCESS;
}

// Callback for SDK Purchase Event handling
OriginErrorT PurchaseEventCallback (int32_t eventId, void* pContext, void* eventData, uint32_t count)
{
   OriginCharT *event = (OriginCharT *)eventData;
   return ORIGIN_SUCCESS;
}

// Callback for SDK Chat Event handling
OriginErrorT ChatEventCallback (int32_t eventId, void* pContext, void* eventData, uint32_t count)
{
   OriginChatT *event = (OriginChatT *)eventData;
   return ORIGIN_SUCCESS;
}

// Callback for SDK Download Event handling
OriginErrorT DownloadEventCallback (int32_t eventId, void* pContext, void* eventData, uint32_t count)
{
   OriginDownloadT *event = (OriginDownloadT *)eventData;
   return ORIGIN_SUCCESS;
}

// Callback for SDK Blocked List Event handling
OriginErrorT BlockListEventCallback (int32_t eventId, void* pContext, void* eventData, uint32_t count)
{
   // Note: eventData is not used for Block List events as the event has no associated data.
   return ORIGIN_SUCCESS;
}