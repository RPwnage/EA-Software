<project>

	<!-- ExceptionHandler header files location. -->
	<property name="package.ExceptionHandler.includedirs">
		${package.ExceptionHandler.dir}/include
	</property>

	<!-- ExceptionHandler libraries location. -->
	<fileset name="package.ExceptionHandler.libs">
		<includes name="${package.ExceptionHandler.builddir}/${config}/lib/${lib-prefix}ExceptionHandler${lib-suffix}" />
	</fileset>

</project>
