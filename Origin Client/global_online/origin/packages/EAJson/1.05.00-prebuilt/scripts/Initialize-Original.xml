<project>

	<!-- EAJson header files location. -->
	<property name="package.EAJson.includedirs">
		${package.EAJson.dir}/include
	</property>

	<!-- EAJson libraries location. -->
    <fileset name="package.EAJson.libs">
        <includes name="${package.EAJson.builddir}/${config}/lib/${lib-prefix}EAJson${lib-suffix}" />
    </fileset>

</project>
