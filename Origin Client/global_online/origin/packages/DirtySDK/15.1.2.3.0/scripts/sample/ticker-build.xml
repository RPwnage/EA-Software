<project>
    <Program name="ticker" frompartial="DSProgram">
        <sourcefiles basedir="${package.dir}">
            <includes fromfile="${package.dir}/sample/ticker/project/${dirtyplat}/source.txt" if="@{FileExists('${package.dir}/sample/ticker/project/${dirtyplat}/source.txt')}"/>
            <includes fromfile="${package.dir}/sample/ticker/project/${dirtyplat}/sourcecpp.txt" if="@{FileExists('${package.dir}/sample/ticker/project/${dirtyplat}/sourcecpp.txt')}" optionset="Program"/>
        </sourcefiles>

        <dependencies>
            <auto>
                DirtySDK/dirtysock

                <do if="${config-system} == 'kettle'">
                    EABase
                    EASTL
                    coreallocator
                    PPMalloc
                    EASystemEventMessageDispatcher
                    EAAssert
                    EAThread
                    IEASystemEventMessageDispatcher
                    EAMessage
                    IEAUser
                    IEAController
                    EAControllerUserPairing
                </do>
            </auto>
        </dependencies>

        <config>
            <warningsuppression>
                <do if="${config-system} == 'winrt' or ${config-system} == 'winprt'">
                    -wd4574 <!-- 'NULL' is defined to be '0': did you mean to use '#if NULL' -->
                </do>

                <do if="${config-compiler} == 'gcc'">
                    -Wno-unused-but-set-variable
                </do>
            </warningsuppression>
        </config>
    </Program>
</project>
