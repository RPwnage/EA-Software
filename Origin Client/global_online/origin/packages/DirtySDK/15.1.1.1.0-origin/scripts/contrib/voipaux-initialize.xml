<!-- note - contrib initialize files must be separate from build files to allow external inclusion -->
<project>

    <!-- add any specific include directories -->
    <property name="package.DirtySDK.voipaux.includedirs">
        ${property.value}
        ${package.DirtySDK.dir}/contrib/voipaux/include
    </property>
</project>
