<project>

	<!-- _PACKAGENAME_ public header files location. -->
	<property name="package._PACKAGENAME_.includedirs">
		${package._PACKAGENAME_.dir}/include
	</property>

	<!-- _PACKAGENAME_ binary files location. -->
	<property name="package._PACKAGENAME_.bindir" value="${package._PACKAGENAME_.dir}/bin" />

	<!-- _PACKAGENAME_ source files location. -->
	<property name="package._PACKAGENAME_.sourcedir" value="${package._PACKAGENAME_.dir}/source" />

	<!-- _PACKAGENAME_ libraries location. -->
	<fileset name="package.engine.libs">
		<includes name="${package._PACKAGENAME_.builddir}/${config}/lib/${lib-prefix}_PACKAGENAME_.lib" />
	</fileset>
</project>
