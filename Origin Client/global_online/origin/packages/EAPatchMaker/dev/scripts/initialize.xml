<project>

    <!-- EAPatchMaker header files location. -->
    <property name="package.EAPatchMaker.includedirs">
        ${package.EAPatchMaker.dir}/include
    </property>

    <!-- EAPatchMaker libraries location. -->
    <fileset name="package.EAPatchMaker.libs">
        <includes name="${package.EAPatchMaker.builddir}/${config}/lib/${lib-prefix}EAPatchMaker${lib-suffix}" />
    </fileset>

</project>
