{
	"name": "otk-custom-glyph",
	"description": "A glyph that takes a single alpha channel image and generates custom colors, drop shadow and inset/etched effects.",
	"dependencies": [
        "otk-base"
    ]
}
