<project>
    <property name="package.EAAssert.includedirs">
        ${package.EAAssert.dir}/include
    </property>

    <property name="package.EAAssert.includedirs.spu">
        ${package.EAAssert.dir}/include
    </property>

    <property name="package.EAAssert.libdir" value="${package.EAAssert.builddir}/${config}/lib"/>

    <fileset name="package.EAAssert.libs">
        <includes name="${package.EAAssert.libdir}/${lib-prefix}EAAssert${lib-suffix}"/>
    </fileset>

</project>
