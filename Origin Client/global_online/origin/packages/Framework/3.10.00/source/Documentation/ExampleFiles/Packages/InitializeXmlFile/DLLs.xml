<project>

  <fileset name="package.MyPackage.dlls">
    <includes name="${package.MyPackage.builddir}/${config}/bin/MyPackage${dll-suffix}" />
    <!-- external prebuilt dll-->
    <includes name="${package.MyPackage.dir}/bin/somedynamiclib.dll" />
  </fileset>

  . . . . . . . . .

</project>