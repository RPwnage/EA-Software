<?xml version="1.0"?>
<example>
<para>Build the BuildServer project located in a different file.  Save result of build to the <c>log</c> property.</para>
<code file="package.build">
<![CDATA[
<project default="build">
    <property name="config" value="debug"/>

    <target name="build">
        <record property="log">
            <nant 
                buildfile="buildserver.xml"
                target="build"
                >
                <property name="config" value="${config}"/>
            </nant>
        </record>
        <!-- ensure build server was built -->
        <fail if="@{StrIndexOf('${log}', 'BuildServer (${config})')} == -1"/>
    </target>
</project>
]]>
</code>
<para>Where <c>BuildServer.xmlc</c> contains:</para>
<code file="buildserver.xml">
<![CDATA[
<project>
    <target name="build">
        <echo message="BuildServer (${config})"/>
    </target>
</project>
]]>
</code>
</example>
