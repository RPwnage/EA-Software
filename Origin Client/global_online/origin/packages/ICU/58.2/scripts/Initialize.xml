<!--
================================================================================
Initialize.xml

NAnt build fragment, which sets up the ${package.ICU.installed} NAnt property.

(c) Electronic Arts Inc, 2002-2013.
================================================================================
-->


<project>

    <!-- Set up a couple of properties. -->
    <property name="package.ICU.installed" value="${package.ICU.dir}/installed"/>
	<property name="package.ICU.includedirs" value="${package.ICU.installed}/include"/>	
	<property name="package.ICU.libdir" value="${package.ICU.installed}/lib" />
	<property name="package.ICU.bindir" value="${package.ICU.installed}/bin" />
</project>
