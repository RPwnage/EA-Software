package com.ea.originx.automation.scripts.zGraveyard.oig.parameterized;

import com.ea.originx.automation.scripts.zGraveyard.oig.OAOigOfflineIndicator;
import com.ea.vx.annotations.TestRail;
import org.testng.ITestContext;
import org.testng.annotations.Test;

/**
 * Parameterized test script of OigOfflineIndicator - Offline mode by going
 * offline via the Origin menu (as oppose to network offline)<br>
 * parameters:<br>
 * networkOfflineTest: false<br>
 * testClassName: name of this class
 *
 * @author palui
 */
public class OAOigOfflineModeIndicator extends OAOigOfflineIndicator {

    @TestRail(caseId = 11299)
    @Test(groups = {"client", "oig", "client_only", "full_regression"})
    public void testOigOfflineModeIndicator(ITestContext context) throws Exception {
        testOigOfflineIndicator(context, false, this.getClass().getName());
    }
}
