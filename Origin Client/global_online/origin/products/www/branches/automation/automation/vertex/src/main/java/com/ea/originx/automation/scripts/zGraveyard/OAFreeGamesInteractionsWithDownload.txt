package com.ea.originx.automation.scripts.server.parameterized;

import com.ea.originx.automation.scripts.server.OAFreeGamesInteractions;
import com.ea.vx.annotations.TestRail;
import org.testng.ITestContext;
import org.testng.annotations.Test;

/**
 * Tests the free games page and checks that the games are directly entitle-able
 *
 * @author caleung
 */
public class OAFreeGamesInteractionsWithDownload extends OAFreeGamesInteractions {

    @TestRail(caseId = 1016725)
    @Test(groups = {"server", "client_only", "services_smoke"})
    public void testFreeGamesInteractionsWithDownloadClient(ITestContext context) throws Exception {
        testFreeGamesInteractions(context, true);
    }
}
