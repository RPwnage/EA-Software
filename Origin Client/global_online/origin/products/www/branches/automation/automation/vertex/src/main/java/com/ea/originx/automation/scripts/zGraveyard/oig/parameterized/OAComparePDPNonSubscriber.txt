package com.ea.originx.automation.scripts.tools.parameterized;

import com.ea.originx.automation.scripts.tools.OAComparePDP;
import org.testng.ITestContext;
import org.testng.annotations.Test;

/**
 * To compare PDP in two different environment for a non
 * subscriber account
 *
 * @author nvarthakavi
 */
public class OAComparePDPNonSubscriber extends OAComparePDP {

    @Test(groups = {"pdp_tools", "browser_only"})
    public void testComparePDPNonSubscriber(ITestContext context) throws Exception {
        testComparePDP(context, false);
    }
}
