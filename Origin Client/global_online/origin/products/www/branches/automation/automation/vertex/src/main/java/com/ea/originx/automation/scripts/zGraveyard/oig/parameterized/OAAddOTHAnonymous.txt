package com.ea.originx.automation.scripts.gdp.parameterized;

import com.ea.originx.automation.scripts.gdp.OAAddOTH;
import com.ea.vx.annotations.TestRail;
import org.testng.ITestContext;
import org.testng.annotations.Test;

/**
 * Test add to game library an entitlement that is on the house for a non
 * subscriber account (browser only)
 *
 * @author cdeaconu
 */
public class OAAddOTHAnonymous extends OAAddOTH{
    
    @TestRail(caseId = 3067871)
    @Test(groups = {"gdp", "freegames", "full_regression", "browser_only"})
    public void testAddOTHAnonymous(ITestContext context) throws Exception{
        testAddOTH(context, OAAddOTH.TEST_TYPE.ANONYMOUS_USER);
    }
}