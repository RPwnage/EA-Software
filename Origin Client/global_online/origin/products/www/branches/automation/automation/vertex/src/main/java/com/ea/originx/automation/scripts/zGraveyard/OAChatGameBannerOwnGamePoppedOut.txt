package com.ea.originx.automation.scripts.social.parameterized;

import com.ea.originx.automation.scripts.social.OAChatGameBanner;
import com.ea.vx.annotations.TestRail;
import org.testng.ITestContext;
import org.testng.annotations.Test;

/**
 * Tests the behaviour of the game banner that appears in the chat window when
 * chatting with a friend who is playing a game that the user owns and when the
 * chat window is popped out
 *
 * @author lscholte
 */
public class OAChatGameBannerOwnGamePoppedOut extends OAChatGameBanner {

    @TestRail(caseId = 12657)
    @Test(groups = {"social", "client_only", "full_regression"})
    public void testChatGameBannerOwnGame(ITestContext context) throws Exception {
        testChatGameBanner(context, true, true);
    }

}
