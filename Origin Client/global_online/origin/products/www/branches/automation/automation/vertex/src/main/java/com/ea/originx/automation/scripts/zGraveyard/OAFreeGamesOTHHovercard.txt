package com.ea.originx.automation.scripts.freegames.parameterized;

import com.ea.originx.automation.scripts.freegames.OAFreeGamesHovercard;
import com.ea.vx.annotations.TestRail;
import org.testng.ITestContext;
import org.testng.annotations.Test;

/**
 * Tests the 'OTH' page hover card on hovering on to 'Get It Now' CTA
 *
 * @author nvarthakavi
 */
public class OAFreeGamesOTHHovercard extends OAFreeGamesHovercard {

    @TestRail(caseId = 12120)
    @Test(groups = {"freegames", "full_regression"})
    public void testFreeGamesOTHHovercard(ITestContext context) throws Exception {
        testFreeGamesHovercard(context, FREE_GAMES_PAGE_TYPE.ON_THE_HOUSE);
    }
}
