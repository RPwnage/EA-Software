package com.ea.originx.automation.scripts.originaccess.parameterized;

import com.ea.originx.automation.scripts.originaccess.OAFreeTrialDiscount;
import com.ea.vx.annotations.TestRail;
import org.testng.ITestContext;
import org.testng.annotations.Test;

/**
 * Tests the appearance of the Origin Access Free Trial discount for subscribers
 *
 * @author sbentley
 */
public class OAFreeTrialDiscountSubscriber extends OAFreeTrialDiscount {

    @TestRail(caseId = 14217)
    @Test(groups = {"originaccess", "full_regression"})
    public void testFreeTrialDiscountSubscriber(ITestContext context) throws Exception {
        testFreeTrialDiscount(context, OAFreeTrialDiscount.ORIGIN_ACCESS.SUBSCRIBER);
    }
}
