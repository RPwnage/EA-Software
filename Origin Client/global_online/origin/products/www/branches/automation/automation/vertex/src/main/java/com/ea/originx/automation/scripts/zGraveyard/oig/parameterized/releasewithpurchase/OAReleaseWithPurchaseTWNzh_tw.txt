package com.ea.originx.automation.scripts.rtb.parameterized.basegame.releasewithpurchase;

import com.ea.originx.automation.scripts.rtb.OAPDPPurchase;
import com.ea.vx.annotations.TestRail;
import com.ea.vx.originclient.resources.CountryInfo;
import com.ea.vx.originclient.resources.LanguageInfo;
import org.testng.ITestContext;
import org.testng.annotations.Test;

/**
 * Verify PDP Release with purchase for TWN/zh-tw locale (for RTB).
 *
 * @author caleung
 */
public class OAReleaseWithPurchaseTWNzh_tw extends OAPDPPurchase {

    @TestRail(caseId = 917115)
    @Test(groups = {"rtb", "rtb_releasewithpurchase", "client_only"})
    public void testReleaseWithPurchaseTWNzh_tw(ITestContext context) throws Exception {
        testRTBPurchase(context, CountryInfo.CountryEnum.TAIWAN, LanguageInfo.LanguageEnum.CHINESE_TRADITIONAL);
    }
}