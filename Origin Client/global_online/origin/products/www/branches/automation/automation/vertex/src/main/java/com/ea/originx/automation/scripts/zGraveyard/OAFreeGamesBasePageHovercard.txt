package com.ea.originx.automation.scripts.freegames.parameterized;

import com.ea.originx.automation.scripts.freegames.OAFreeGamesHovercard;
import com.ea.vx.annotations.TestRail;
import org.testng.ITestContext;
import org.testng.annotations.Test;

/**
 * Tests the 'Free Games' page cover Card on hovering on to 'Add to Game Library' CTA
 *
 * @author nvarthakavi
 */
public class OAFreeGamesBasePageHovercard extends OAFreeGamesHovercard {

    @TestRail(caseId = 12119)
    @Test(groups = {"freegames", "full_regression"})
    public void testFreeGamesBasePageHovercard(ITestContext context) throws Exception {
        testFreeGamesHovercard(context, FREE_GAMES_PAGE_TYPE.FREE_GAMES_BASE_PAGE);
    }
}
