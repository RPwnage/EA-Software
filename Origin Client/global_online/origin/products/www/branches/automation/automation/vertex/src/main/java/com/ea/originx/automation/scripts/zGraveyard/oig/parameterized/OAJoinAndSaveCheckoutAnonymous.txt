package com.ea.originx.automation.scripts.checkout.parameterized;

import com.ea.originx.automation.scripts.checkout.OAJoinAndSaveCheckout;
import com.ea.vx.annotations.TestRail;
import org.testng.ITestContext;
import org.testng.annotations.Test;

/**
 * Tests the 'Join and Save' checkout flow as an anonymous user
 *
 * @author caleung
 */
public class OAJoinAndSaveCheckoutAnonymous extends OAJoinAndSaveCheckout {

    @TestRail(caseId = 1295656)
    @Test(groups = {"checkout", "full_regression", "browser_only"})
    public void testOAJoinAndSaveCheckoutAnonymous(ITestContext context) throws Exception {
        testJoinAndSaveCheckout(context, TEST_TYPE.ANONYMOUS_USER);
    }
}
