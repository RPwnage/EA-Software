package com.ea.originx.automation.scripts.freegames.parameterized;

import com.ea.originx.automation.scripts.freegames.OAFreeGamesHovercard;
import com.ea.vx.annotations.TestRail;
import org.testng.ITestContext;
import org.testng.annotations.Test;

/**
 * Tests the 'Trials' Page hover card on hovering on to 'Get It Now' CTA
 *
 * @author nvarthakavi
 */
public class OAFreeGamesTrialsHovercard extends OAFreeGamesHovercard {

    @TestRail(caseId = 36982)
    @Test(groups = {"freegames", "full_regression"})
    public void testFreeGamesTrialsHovercard(ITestContext context) throws Exception {
        testFreeGamesHovercard(context, FREE_GAMES_PAGE_TYPE.TRIALS);
    }
}
