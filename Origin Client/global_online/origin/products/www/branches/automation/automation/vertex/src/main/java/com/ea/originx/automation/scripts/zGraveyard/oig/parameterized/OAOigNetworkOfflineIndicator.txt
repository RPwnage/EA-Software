package com.ea.originx.automation.scripts.zGraveyard.oig.parameterized;

import com.ea.originx.automation.scripts.zGraveyard.oig.OAOigOfflineIndicator;
import com.ea.vx.annotations.TestRail;
import org.testng.ITestContext;
import org.testng.annotations.Test;

/**
 * Parameterized test script of OigOfflineIndicator - Offline mode by blocking
 * Origin with firewall (as oppose to going offline via the main menu)<br>
 * parameters:<br>
 * networkOfflineTest: true<br>
 * testClassName: name of this class
 *
 * @author palui
 */
public class OAOigNetworkOfflineIndicator extends OAOigOfflineIndicator {

    @TestRail(caseId = 11282)
    @Test(groups = {"oig", "client_only", "full_regression"})
    public void testOigOfflineModeIndicator(ITestContext context) throws Exception {
        testOigOfflineIndicator(context, true, this.getClass().getName());
    }
}
