package com.ea.originx.automation.scripts.zGraveyard.oig.parameterized;

import com.ea.originx.automation.scripts.zGraveyard.oig.OAOigNavigationLaunch;
import com.ea.vx.annotations.TestRail;
import org.testng.ITestContext;
import org.testng.annotations.Test;

/**
 * Test all navigation links for the OIG for full regression
 *
 * @author cvanichsarn
 */
public class OAOigNavigationLaunchOrigin extends OAOigNavigationLaunch {

    @TestRail(caseId = 11144)
    @Test(groups = {"oig", "client_only", "full_regression"})
    public void testOigNavigationLaunchOrigin(ITestContext context) throws Exception {
        testOigNavigationLaunch(context, false);
    }
}
