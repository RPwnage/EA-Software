package com.ea.originx.automation.scripts.checkout.parameterized;

import com.ea.originx.automation.scripts.checkout.OAJoinAndSaveFooter;
import com.ea.vx.annotations.TestRail;
import org.testng.ITestContext;
import org.testng.annotations.Test;

/**
 * Tests the 'Join and Save' footer as an anonymous user
 *
 * @author caleung
 */
public class OAJoinAndSaveFooterAnonymous extends OAJoinAndSaveFooter {

    @TestRail(caseId = 1016759)
    @Test(groups = {"checkout", "full_regression", "browser_only"})
    public void testOAJoinAndSaveFooterAnonymous(ITestContext context) throws Exception {
        testJoinAndSaveFooter(context, TEST_TYPE.ANONYMOUS_USER);
    }
}
