package com.ea.originx.automation.scripts.rtb.parameterized.basegame.releasewithpurchase;

import com.ea.originx.automation.scripts.rtb.OAPDPPurchase;
import com.ea.vx.annotations.TestRail;
import com.ea.vx.originclient.resources.CountryInfo;
import com.ea.vx.originclient.resources.LanguageInfo;
import org.testng.ITestContext;
import org.testng.annotations.Test;

/**
 * Verify PDP Release with purchase for NLD/nl-nl locale (for RTB).
 *
 * @author caleung
 */
public class OAReleaseWithPurchaseNLDnl_nl extends OAPDPPurchase {

    @TestRail(caseId = 917107)
    @Test(groups = {"rtb", "rtb_releasewithpurchase", "client_only"})
    public void testReleaseWithPurchaseNLDnl_nl(ITestContext context) throws Exception {
        testRTBPurchase(context, CountryInfo.CountryEnum.NETHERLANDS, LanguageInfo.LanguageEnum.DUTCH);
    }
}