package com.ea.originx.automation.scripts.rtb.parameterized.oth;

import com.ea.originx.automation.scripts.rtb.OAOnTheHouse;
import com.ea.vx.originclient.resources.CountryInfo;
import com.ea.vx.originclient.resources.LanguageInfo;
import java.lang.invoke.MethodHandles;
import org.testng.ITestContext;
import org.testng.annotations.Test;

/**
 * Tests the 'On the House' Entitlements in Country 'nor' (Norway) and Language 'no-no' (Norwegian)
 *
 * @author nvarthakavi
 */
public class OAOnTheHouseNORno_no extends OAOnTheHouse {

    @Test(groups = {"rtb", "rtb_oth", "client_only"})
    public void testOnTheHouseNORno_no(ITestContext context) throws Exception {
        String testName = MethodHandles.lookup().lookupClass().toString().split(" ")[1];
        testOnTheHouse(context, CountryInfo.CountryEnum.NORWAY, LanguageInfo.LanguageEnum.NORWEGIAN, testName);
    }
}
