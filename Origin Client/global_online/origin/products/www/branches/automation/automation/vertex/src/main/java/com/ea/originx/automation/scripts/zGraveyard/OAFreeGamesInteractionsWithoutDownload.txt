package com.ea.originx.automation.scripts.server.parameterized;

import com.ea.originx.automation.scripts.server.OAFreeGamesInteractions;
import com.ea.vx.annotations.TestRail;
import org.testng.ITestContext;
import org.testng.annotations.Test;

/**
 * Tests the free games page and checks that the games are directly entitle-able
 *
 * @author caleung
 */
public class OAFreeGamesInteractionsWithoutDownload extends OAFreeGamesInteractions {

    @TestRail(caseId = 12080)
    @Test(groups = {"server", "full_regression"})
    public void testFreeGamesInteractionsBrowser(ITestContext context) throws Exception {
        testFreeGamesInteractions(context, false);
    }

}
