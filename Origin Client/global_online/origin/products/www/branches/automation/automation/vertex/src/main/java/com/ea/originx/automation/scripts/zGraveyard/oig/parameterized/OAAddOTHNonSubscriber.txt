package com.ea.originx.automation.scripts.gdp.parameterized;

import com.ea.originx.automation.scripts.gdp.OAAddOTH;
import com.ea.vx.annotations.TestRail;
import org.testng.ITestContext;
import org.testng.annotations.Test;

/**
 * Test add to game library an entitlement that is on the house for a non
 * subscriber account
 *
 * @author cdeaconu
 */
public class OAAddOTHNonSubscriber extends OAAddOTH{
    
    @TestRail(caseId = 3067872)
    @Test(groups = {"gdp", "freegames", "full_regression"})
    public void testAddOTHNonSubscriber(ITestContext context) throws Exception{
        testAddOTH(context, OAAddOTH.TEST_TYPE.NON_SUBSCRIBER);
    }
}