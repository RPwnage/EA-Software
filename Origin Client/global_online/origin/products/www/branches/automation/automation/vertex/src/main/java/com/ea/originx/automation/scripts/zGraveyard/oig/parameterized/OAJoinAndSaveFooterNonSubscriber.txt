package com.ea.originx.automation.scripts.checkout.parameterized;

import com.ea.originx.automation.scripts.checkout.OAJoinAndSaveFooter;
import com.ea.vx.annotations.TestRail;
import org.testng.ITestContext;
import org.testng.annotations.Test;

/**
 * Tests the 'Join and Save' footer as an a non-subscriber
 *
 * @author caleung
 */
public class OAJoinAndSaveFooterNonSubscriber extends OAJoinAndSaveFooter {

    @TestRail(caseId = 1289059)
    @Test(groups = {"checkout", "full_regression"})
    public void testOAJoinAndSaveFooterNonSubscriber(ITestContext context) throws Exception {
        testJoinAndSaveFooter(context, TEST_TYPE.NON_SUBSCRIBER);
    }
}
