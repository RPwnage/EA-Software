package com.ea.originx.automation.scripts.gdp.parameterized;

import com.ea.originx.automation.scripts.gdp.OAAddOTH;
import com.ea.vx.annotations.TestRail;
import org.testng.ITestContext;
import org.testng.annotations.Test;

/**
 * Test add to game library an entitlement that is on the house for a subscriber
 * account
 *
 * @author cdeaconu
 */
public class OAAddOTHSubscriber extends OAAddOTH{
    
    @TestRail(caseId = 3087357)
    @Test(groups = {"gdp", "freegames", "full_regression"})
    public void testAddOTHSubscriber(ITestContext context) throws Exception{
        testAddOTH(context, OAAddOTH.TEST_TYPE.SUBSCRIBER);
    }
}