package com.ea.originx.automation.scripts.social.parameterized;

import com.ea.originx.automation.scripts.social.OAChatGameBanner;
import org.testng.ITestContext;
import org.testng.annotations.Test;

/**
 * Tests the behaviour of the game banner that appears in the chat window when
 * chatting with a friend who is playing a game that the user does not own and
 * when the chat window is popped out
 *
 * @author lscholte
 */
public class OAChatGameBannerNotOwnGamePoppedOut extends OAChatGameBanner {

    @Test(groups = {"social", "client_only"})
    public void testChatGameBannerNotOwnGamePoppedOut(ITestContext context) throws Exception {
        testChatGameBanner(context, false, true);
        //Do not run this test case until non-Dip small has a store page
    }

}
