//
// This is the main association list configuration file.
//

// In the case adding a new member to a full list is full, we will roll
// the new member in based on FIFO if rollover for the list is set to
// true(if not specified, default is false), otherwise, will error out.

// List name and id(uint32) should be unique in the scope

component.associationlists = {
      
      // Database configuration
      dbNamesByPlatform = {
          "#PLATFORM#" = "#ASSOCIATIONLIST_DB_ALIAS#"
      }

      lists = [
        {
            // FIFA SPECIFIC CODE START
            name = "OSDKPlatformFriendList",
            // FIFA SPECIFIC CODE END
            id = 1,
            maxSize = 5000,
            loadOfflineUED = false
        },
        {
            name = "recentPlayerList",
            id = 2,
            maxSize = 100,
            rollover = true
        },
        {
            name = "persistentMuteList",
            id = 3,
            rollover = true,
            maxSize = 100   
        },
        {
            name = "communicationBlockList",
            id = 4,
            maxSize = 5000
        },
        // FIFA SPECIFIC CODE START
        {
            name = "OSDKPreferredPlayerList",
            id = 5,
            maxSize = 100
        },
        {
            name = "OSDKAvoidPlayerList",
            id = 6,
            maxSize = 100,
			mutualAction = true
        }
        // FIFA SPECIFIC CODE END
      ]
}
