"crossplayFindGame" = {
    totalDuration = "15s"
    subsessions = {
        "quickMatchSession" = {                         // Sub session names are arbitrary. Displayed via GOSCC.
            rulesList = [ "normalPlayerCountRule",
                          "configCrossplayRule"
                        ]
            matchmakingSettings = {
                sessionMode = "FindGame"
            }
        }
    }
}

"crossplayCreateGame" = {
    totalDuration = "15s"
    subsessions = {
        
        "quickMatchSession" = {                         // Sub session names are arbitrary. Displayed via GOSCC.

            rulesList = [ "normalPlayerCountRule",      // Find a game with the desired number of players (~4)
                          "createGameTotalPlayerSlotsRule",
                          "configCrossplayRule"
                        ]
 
            matchmakingSettings = {
                sessionMode = "CreateGame"
            }

            // The CreateGameTemplate will be used as the basis for the create game request:
            createGameTemplate = "crossplayDedicatedServerReset"
        }
    }
}

"singlePlatformMM" = {
    totalDuration = "15s"
    subsessions = {
        
        "quickMatchSession" = {                         // Sub session names are arbitrary. Displayed via GOSCC.

            rulesList = [ "normalPlayerCountRule",      // Find a game with the desired number of players (~4)
                          "createGameTotalPlayerSlotsRule"
                        ]
 
            matchmakingSettings = {
                sessionMode = "CreateGame"
            }

            // The CreateGameTemplate will be used as the basis for the create game request:
            createGameTemplate = "singlePlatformDedicatedServerReset"
        }
    }
}
