<project xmlns="schemas/ea/framework3.xsd">

  <include file="exclude.xml"/>
  
  <property name="exclude.modules.tokenized" value=";"/>

  <foreach item="String" in="${exclude.modules}" property="token">
    <property name="exclude.modules.tokenized" value="${property.value}${token};"/>
  </foreach>

</project>
