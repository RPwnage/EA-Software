<project xmlns="schemas/ea/framework3.xsd">

  <publicdata packagename="GamePacker" >
    
    <!-- runtime group is default and can be skipped -->
    <runtime>
      
      <module name="GamePacker">
        <includedirs>
          ${package.GamePacker.dir}/include
        </includedirs>
        <!-- Empty 'lib' element tells the task to add default library name based on the module name. -->
        <libs/>
      </module>
      
    </runtime>
  </publicdata>

</project>