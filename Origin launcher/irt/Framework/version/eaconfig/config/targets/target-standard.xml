<!-- 
     (c) Electronic Arts. All Rights Reserved.
-->
<project xmlns="schemas/ea/framework3.xsd">

  <!-- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -->
  <do unless="'@{OptionSetGetValue('config.targetoverrides', 'post')}' == 'exclude'">
    <target name="post" description="Post zip to package server" depends="package"  allowoverride="true">
      <copy file="${package.builddir}/${package.name}-${package.targetversion}.zip" todir="\\eac-as5.eac.ad.ea.com\incoming"/>
    </target>
  </do>

  <!-- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -->
  <do unless="'@{OptionSetGetValue('config.targetoverrides', 'post-external')}' == 'exclude'">
    <target name="post-external" description="Post external zip to package server" depends="package-external"  allowoverride="true">
      <copy file="${package.builddir}/${package.name}-external-${package.targetversion}.zip" todir="\\eac-as5.eac.ad.ea.com\incoming"/>
    </target>
  </do>

</project>
