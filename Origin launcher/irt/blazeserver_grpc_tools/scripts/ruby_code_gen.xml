<project>

    <createtask name="messages.gen.ruby">
        <parameters>
            <option name="rootdir"      value="Required"/> <!-- The root directory where all the proto files are located. ProtoC needs the files to be processed from same directory structure so we create a common root. -->
            <option name="inputdir"     value="Required"/> <!-- The directory whose proto files you want to process. All proto files (including subdirectories) are processed. Normally, same as rootdir but can be different if you want to process input proto files in different directories separately. -->
            <option name="outputdir"    value="Required"/> <!-- The directory where you want to output the generated code. -->
            <option name="depfilename"  value="Required"/> <!-- The dependency file name to use.-->
        </parameters>

        <code>
            <mkdir dir="${messages.gen.ruby.outputdir}"/>
            <task name="generate_relative_proto_paths" inputdir="${messages.gen.ruby.inputdir}" rootdir="${messages.gen.ruby.rootdir}"/>

            <exec program="${protoc.exe}" workingdir="${messages.gen.ruby.rootdir}">
                <args>
                    <arg value="--proto_path=."/>
                    <arg value="--proto_path=${package.protobuf.dir}/src"/>
                    <arg value="--ruby_out=${messages.gen.ruby.outputdir}"/>
                    <arg value="${proto.files.relative}"/>
                </args>
                <inputs>
                    <includes name="${messages.gen.ruby.inputdir}/**.proto"/>
                    <includes name="${protoc.exe}"/>
                </inputs>
                <outputs>
                    <includes name="${messages.gen.ruby.outputdir}/${messages.gen.ruby.depfilename}" />
                </outputs>
            </exec>
            <touch file="${messages.gen.ruby.outputdir}/${messages.gen.ruby.depfilename}"/>
        </code>
    </createtask>

    <createtask name="services.gen.ruby">
        <parameters>
            <option name="rootdir"      value="Required"/> <!-- The root directory where all the proto files are located. ProtoC needs the files to be processed from same directory structure so we create a common root. -->
            <option name="inputdir"     value="Required"/> <!-- The directory whose proto files you want to process. All proto files (including subdirectories) are processed. Normally, same as rootdir but can be different if you want to process input proto files in different directories separately. -->
            <option name="outputdir"    value="Required"/> <!-- The directory where you want to output the generated code. -->
            <option name="depfilename"  value="Required"/> <!-- The dependency file name to use.-->
        </parameters>
        <code>
            <mkdir dir="${services.gen.ruby.outputdir}"/>
            <task name="generate_relative_proto_paths" inputdir="${services.gen.ruby.inputdir}" rootdir="${services.gen.ruby.rootdir}"/>

            <exec program="${protoc.exe}" workingdir="${services.gen.ruby.rootdir}">
                <args>
                    <arg value="--proto_path=."/>
                    <arg value="--proto_path=${package.protobuf.dir}/src"/>
                    <arg value="--grpc_out=${services.gen.ruby.outputdir}"/>
                    <arg value="--plugin=protoc-gen-grpc=${grpc_ruby_plugin.exe}"/>
                    <arg value="${proto.files.relative}"/>
                </args>
                <inputs>
                    <includes name="${services.gen.ruby.inputdir}/**.proto"/>
                    <includes name="${protoc.exe}"/>
                    <includes name="${grpc_ruby_plugin.exe}"/>
                </inputs>
                <outputs>
                    <includes name="${services.gen.ruby.outputdir}/${services.gen.ruby.depfilename}" />
                </outputs>
            </exec>
            <touch file="${services.gen.ruby.outputdir}/${services.gen.ruby.depfilename}"/>
        </code>
    </createtask>
</project>