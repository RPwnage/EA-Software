namespace Blaze
{
namespace Nucleus
{

namespace EntitlementStatus
{
    [description="Nucleus entitlement status"]
    enum Code
    {
        [description="Unknown"]
        UNKNOWN,
        [description="Entitlement is active"]
        ACTIVE,
        [description="Entitlement is disabled"]
        DISABLED,
        [description="Entitlement is pending"]
        PENDING,
        [description="Entitlement is deleted"]
        DELETED,
        [description="Entitlement is banned"]
        BANNED
    };
}

}
}