<project xmlns="schemas/ea/framework3.xsd">
	<publicdata packagename="Framework" configbindir="${package.Framework.dir}/bin">
		<module name="Framework">
			<assemblies-external>
				<includes name="${package.Framework.dir}/bin/CredentialManager.exe"/> <!-- needs to be located alongside the authentaticion dll for things like fbpackages -->
				<includes name="${package.Framework.dir}/bin/EA.*.dll"/>
				<includes name="${package.Framework.dir}/bin/Google.*.dll"/>
				<includes name="${package.Framework.dir}/bin/NAnt.*.dll"/>
				<includes name="${package.Framework.dir}/bin/ProtocolPackageServer.dll"/>
				<includes name="${package.Framework.dir}/bin/WebServicePackageServer.dll"/>
			</assemblies-external>
		</module>
	</publicdata>
</project>
