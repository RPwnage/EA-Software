// The enum names below match strings used by Nucleus, so the generated CodeToString functions will
// return valid Nucleus values. The UKNOWN values serve as defaults for optional parameters.
//
// Corresponding stringToCode functions are in nucleusstringtocode.cpp. They are not auto-generated,
// so be sure to update them if you edit the enums.
//
// These definitions are separate from accountdefines.tdf to prevent the C++ generator from defining
// them after they're referenced.

[headername="authentication/nucleuscodes/tdf/accountstatus.h"]
#include "authentication/gen/nucleuscodes/accountstatus.tdf"

[headername="authentication/nucleuscodes/tdf/emailstatus.h"]
#include "authentication/gen/nucleuscodes/emailstatus.tdf"

[headername="authentication/nucleuscodes/tdf/entitlementsearchtype.h"]
#include "authentication/gen/nucleuscodes/entitlementsearchtype.tdf"

[headername="authentication/nucleuscodes/tdf/entitlementstatus.h"]
#include "authentication/gen/nucleuscodes/entitlementstatus.tdf"

[headername="authentication/nucleuscodes/tdf/entitlementtype.h"]
#include "authentication/gen/nucleuscodes/entitlementtype.tdf"

[headername="authentication/nucleuscodes/tdf/nucleuscause.h"]
#include "authentication/gen/nucleuscodes/nucleuscause.tdf"

[headername="authentication/nucleuscodes/tdf/nucleuscode.h"]
#include "authentication/gen/nucleuscodes/nucleuscode.tdf"

[headername="authentication/nucleuscodes/tdf/nucleusfield.h"]
#include "authentication/gen/nucleuscodes/nucleusfield.tdf"

[headername="authentication/nucleuscodes/tdf/personastatus.h"]
#include "authentication/gen/nucleuscodes/personastatus.tdf"

[headername="authentication/nucleuscodes/tdf/productcatalog.h"]
#include "authentication/gen/nucleuscodes/productcatalog.tdf"

[headername="authentication/nucleuscodes/tdf/statusreason.h"]
#include "authentication/gen/nucleuscodes/statusreason.tdf"

namespace Blaze
{
namespace Authentication
{
}
}