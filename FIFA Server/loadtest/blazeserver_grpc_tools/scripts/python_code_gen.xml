<project>

    <createtask name="messages.gen.python">
        <parameters>
            <option name="rootdir"      value="Required"/> <!-- The root directory where all the proto files are located. ProtoC needs the files to be processed from same directory structure so we create a common root. -->
            <option name="inputdir"     value="Required"/> <!-- The directory whose proto files you want to process. All proto files (including subdirectories) are processed. Normally, same as rootdir but can be different if you want to process input proto files in different directories separately. -->
            <option name="outputdir"    value="Required"/> <!-- The directory where you want to output the generated code. -->
            <option name="depfilename"  value="Required"/> <!-- The dependency file name to use.-->
        </parameters>

        <code>
            <mkdir dir="${messages.gen.python.outputdir}"/>
            <task name="generate_relative_proto_paths" inputdir="${messages.gen.python.inputdir}" rootdir="${messages.gen.python.rootdir}"/>

            <exec program="${protoc.exe}" workingdir="${messages.gen.python.rootdir}">
                <args>
                    <arg value="--proto_path=."/>
                    <arg value="--proto_path=${package.protobuf.dir}/src"/>
                    <arg value="--python_out=${messages.gen.python.outputdir}"/>
                    <arg value="${proto.files.relative}"/>
                </args>
                <inputs>
                    <includes name="${messages.gen.python.inputdir}/**.proto"/>
                    <includes name="${protoc.exe}"/>
                </inputs>
                <outputs>
                    <includes name="${messages.gen.python.outputdir}/${messages.gen.python.depfilename}" />
                </outputs>
            </exec>
            <touch file="${messages.gen.python.outputdir}/${messages.gen.python.depfilename}"/>
        </code>
    </createtask>

    <createtask name="services.gen.python">
        <parameters>
            <option name="rootdir"      value="Required"/> <!-- The root directory where all the proto files are located. ProtoC needs the files to be processed from same directory structure so we create a common root. -->
            <option name="inputdir"     value="Required"/> <!-- The directory whose proto files you want to process. All proto files (including subdirectories) are processed. Normally, same as rootdir but can be different if you want to process input proto files in different directories separately. -->
            <option name="outputdir"    value="Required"/> <!-- The directory where you want to output the generated code. -->
            <option name="depfilename"  value="Required"/> <!-- The dependency file name to use.-->
        </parameters>
        <code>
            <mkdir dir="${services.gen.python.outputdir}"/>
            <task name="generate_relative_proto_paths" inputdir="${services.gen.python.inputdir}" rootdir="${services.gen.python.rootdir}"/>

            <exec program="${protoc.exe}" workingdir="${services.gen.python.rootdir}">
                <args>
                    <arg value="--proto_path=."/>
                    <arg value="--proto_path=${package.protobuf.dir}/src"/>
                    <arg value="--python_out=${services.gen.python.outputdir}"/>
                    <arg value="--grpc_out=${services.gen.python.outputdir}"/>
                    <arg value="--plugin=protoc-gen-grpc=${grpc_python_plugin.exe}"/>
                    <arg value="${proto.files.relative}"/>
                </args>
                <inputs>
                    <includes name="${services.gen.python.inputdir}/**.proto"/>
                    <includes name="${protoc.exe}"/>
                    <includes name="${grpc_python_plugin.exe}"/>
                </inputs>
                <outputs>
                    <includes name="${services.gen.python.outputdir}/${services.gen.python.depfilename}" />
                </outputs>
            </exec>
            <touch file="${services.gen.python.outputdir}/${services.gen.python.depfilename}"/>
        </code>
    </createtask>
</project>