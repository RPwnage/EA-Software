<project>

    <createtask name="messages.gen.objc">
        <parameters>
            <option name="rootdir"      value="Required"/> <!-- The root directory where all the proto files are located. ProtoC needs the files to be processed from same directory structure so we create a common root. -->
            <option name="inputdir"     value="Required"/> <!-- The directory whose proto files you want to process. All proto files (including subdirectories) are processed. Normally, same as rootdir but can be different if you want to process input proto files in different directories separately. -->
            <option name="outputdir"    value="Required"/> <!-- The directory where you want to output the generated code. -->
            <option name="depfilename"  value="Required"/> <!-- The dependency file name to use.-->
        </parameters>

        <code>
            <mkdir dir="${messages.gen.objc.outputdir}"/>
            <task name="generate_relative_proto_paths" inputdir="${messages.gen.objc.inputdir}" rootdir="${messages.gen.objc.rootdir}"/>

            <exec program="${protoc.exe}" workingdir="${messages.gen.objc.rootdir}">
                <args>
                    <arg value="--proto_path=."/>
                    <arg value="--proto_path=${package.protobuf.dir}/src"/>
                    <arg value="--objc_out=${messages.gen.objc.outputdir}"/>
                    <arg value="${proto.files.relative}"/>
                </args>
                <inputs>
                    <includes name="${messages.gen.objc.inputdir}/**.proto"/>
                    <includes name="${protoc.exe}"/>
                </inputs>
                <outputs>
                    <includes name="${messages.gen.objc.outputdir}/${messages.gen.objc.depfilename}" />
                </outputs>
            </exec>
            <touch file="${messages.gen.objc.outputdir}/${messages.gen.objc.depfilename}"/>
        </code>
    </createtask>

    <createtask name="services.gen.objc">
        <parameters>
            <option name="rootdir"      value="Required"/> <!-- The root directory where all the proto files are located. ProtoC needs the files to be processed from same directory structure so we create a common root. -->
            <option name="inputdir"     value="Required"/> <!-- The directory whose proto files you want to process. All proto files (including subdirectories) are processed. Normally, same as rootdir but can be different if you want to process input proto files in different directories separately. -->
            <option name="outputdir"    value="Required"/> <!-- The directory where you want to output the generated code. -->
            <option name="depfilename"  value="Required"/> <!-- The dependency file name to use.-->
        </parameters>
        <code>
            <mkdir dir="${services.gen.objc.outputdir}"/>
            <task name="generate_relative_proto_paths" inputdir="${services.gen.objc.inputdir}" rootdir="${services.gen.objc.rootdir}"/>

            <exec program="${protoc.exe}" workingdir="${services.gen.objc.rootdir}">
                <args>
                    <arg value="--proto_path=."/>
                    <arg value="--proto_path=${package.protobuf.dir}/src"/>
                    <arg value="--grpc_out=${services.gen.objc.outputdir}"/>
                    <arg value="--plugin=protoc-gen-grpc=${grpc_objc_plugin.exe}"/>
                    <arg value="${proto.files.relative}"/>
                </args>
                <inputs>
                    <includes name="${services.gen.objc.inputdir}/**.proto"/>
                    <includes name="${protoc.exe}"/>
                    <includes name="${grpc_objc_plugin.exe}"/>
                </inputs>
                <outputs>
                    <includes name="${services.gen.objc.outputdir}/${services.gen.objc.depfilename}" />
                </outputs>
            </exec>
            <touch file="${services.gen.objc.outputdir}/${services.gen.objc.depfilename}"/>
        </code>
    </createtask>
</project>