<project>
    <createtask name="messages.gen.csharp">
        <parameters>
            <option name="rootdir"      value="Required"/> <!-- The root directory where all the proto files are located. ProtoC needs the files to be processed from same directory structure so we create a common root. -->
            <option name="inputdir"     value="Required"/> <!-- The directory whose proto files you want to process. All proto files (including subdirectories) are processed. Normally, same as rootdir but can be different if you want to process input proto files in different directories separately. -->
            <option name="outputdir"    value="Required"/> <!-- The directory where you want to output the generated code. -->
            <option name="csharpopt" value="Required"/>
            <option name="depfilename"  value="Required"/> <!-- The dependency file name to use.-->
        </parameters>
        
        <code>
            <mkdir dir="${messages.gen.csharp.outputdir}"/>
            <task name="generate_relative_proto_paths" inputdir="${messages.gen.csharp.inputdir}" rootdir="${messages.gen.csharp.rootdir}"/>

            <exec program="${protoc.exe}" workingdir="${messages.gen.csharp.rootdir}">
                <args>
                    <arg value="--proto_path=."/>
                    <arg value="--proto_path=${package.protobuf.dir}/src"/>
                    <arg value="--csharp_opt=${messages.gen.csharp.csharpopt}"/>
                    <arg value="--csharp_out=${messages.gen.csharp.outputdir}"/>
                    <arg value="${proto.files.relative}"/>
                </args>
                <inputs>
                    <includes name="${messages.gen.csharp.inputdir}/**.proto"/>
                    <includes name="${protoc.exe}"/>
                </inputs>
                <outputs>
                    <includes name="${messages.gen.csharp.outputdir}/${messages.gen.csharp.depfilename}" />
                </outputs>
            </exec>
            <touch file="${messages.gen.csharp.outputdir}/${messages.gen.csharp.depfilename}"/>
        </code>
    </createtask>

    <createtask name="services.gen.csharp">
        <parameters>
            <option name="rootdir"      value="Required"/> <!-- The root directory where all the proto files are located. ProtoC needs the files to be processed from same directory structure so we create a common root. -->
            <option name="inputdir"     value="Required"/> <!-- The directory whose proto files you want to process. All proto files (including subdirectories) are processed. Normally, same as rootdir but can be different if you want to process input proto files in different directories separately. -->
            <option name="outputdir"    value="Required"/> <!-- The directory where you want to output the generated code. -->
            <option name="csharpopt" value="Required"/>
            <option name="depfilename"  value="Required"/> <!-- The dependency file name to use.-->
        </parameters>
        
        <code>
            <mkdir dir="${services.gen.csharp.outputdir}"/>
            <task name="generate_relative_proto_paths" inputdir="${services.gen.csharp.inputdir}" rootdir="${services.gen.csharp.rootdir}"/>

            <exec program="${protoc.exe}" workingdir="${services.gen.csharp.rootdir}">
                <args>
                    <arg value="--proto_path=."/>
                    <arg value="--proto_path=${package.protobuf.dir}/src"/>
                    <arg value="--grpc_out=${services.gen.csharp.outputdir}"/>
                    <arg value="--csharp_opt=${services.gen.csharp.csharpopt}"/>
                    <arg value="--plugin=protoc-gen-grpc=${grpc_csharp_plugin.exe}"/>
                    <arg value="${proto.files.relative}"/>
                </args>
                <inputs>
                    <includes name="${services.gen.csharp.inputdir}/**.proto"/>
                    <includes name="${protoc.exe}"/>
                    <includes name="${grpc_csharp_plugin.exe}"/>
                </inputs>
                <outputs>
                    <includes name="${services.gen.csharp.outputdir}/${services.gen.csharp.depfilename}" />
                </outputs>
            </exec>
            <touch file="${services.gen.csharp.outputdir}/${services.gen.csharp.depfilename}"/>
        </code>
    </createtask>
    
</project>
  