<project>

    <createtask name="messages.gen.js">
        <parameters>
            <option name="rootdir"      value="Required"/> <!-- The root directory where all the proto files are located. ProtoC needs the files to be processed from same directory structure so we create a common root. -->
            <option name="inputdir"     value="Required"/> <!-- The directory whose proto files you want to process. All proto files (including subdirectories) are processed. Normally, same as rootdir but can be different if you want to process input proto files in different directories separately. -->
            <option name="outputdir"    value="Required"/> <!-- The directory where you want to output the generated code. -->
            <option name="depfilename"  value="Required"/> <!-- The dependency file name to use.-->
        </parameters>

        <code>
            <mkdir dir="${messages.gen.js.outputdir}"/>
            <task name="generate_relative_proto_paths" inputdir="${messages.gen.js.inputdir}" rootdir="${messages.gen.js.rootdir}"/>

            <exec program="${protoc.exe}" workingdir="${messages.gen.js.rootdir}">
                <args>
                    <arg value="--proto_path=."/>
                    <arg value="--proto_path=${package.protobuf.dir}/src"/>
                    <arg value="--js_out=import_style=commonjs,binary:${messages.gen.js.outputdir}"/>
                    <arg value="${proto.files.relative}"/>
                </args>
                <inputs>
                    <includes name="${messages.gen.js.inputdir}/**.proto"/>
                    <includes name="${protoc.exe}"/>
                </inputs>
                <outputs>
                    <includes name="${messages.gen.js.outputdir}/${messages.gen.js.depfilename}" />
                </outputs>
            </exec>
            <touch file="${messages.gen.js.outputdir}/${messages.gen.js.depfilename}"/>
        </code>
    </createtask>

    <createtask name="services.gen.js">
        <parameters>
            <option name="rootdir"      value="Required"/> <!-- The root directory where all the proto files are located. ProtoC needs the files to be processed from same directory structure so we create a common root. -->
            <option name="inputdir"     value="Required"/> <!-- The directory whose proto files you want to process. All proto files (including subdirectories) are processed. Normally, same as rootdir but can be different if you want to process input proto files in different directories separately. -->
            <option name="outputdir"    value="Required"/> <!-- The directory where you want to output the generated code. -->
            <option name="depfilename"  value="Required"/> <!-- The dependency file name to use.-->
        </parameters>
        <code>
            <mkdir dir="${services.gen.js.outputdir}"/>
            <task name="generate_relative_proto_paths" inputdir="${services.gen.js.inputdir}" rootdir="${services.gen.js.rootdir}"/>

            <exec program="${protoc.exe}" workingdir="${services.gen.js.rootdir}">
                <args>
                    <arg value="--proto_path=."/>
                    <arg value="--proto_path=${package.protobuf.dir}/src"/>
                    <arg value="--grpc_out=${services.gen.js.outputdir}"/>
                    <arg value="--plugin=protoc-gen-grpc=${grpc_node_plugin.exe}"/>
                    <arg value="${proto.files.relative}"/>
                </args>
                <inputs>
                    <includes name="${services.gen.js.inputdir}/**.proto"/>
                    <includes name="${protoc.exe}"/>
                    <includes name="${grpc_node_plugin.exe}"/>
                </inputs>
                <outputs>
                    <includes name="${services.gen.js.outputdir}/${services.gen.js.depfilename}" />
                </outputs>
            </exec>
            <touch file="${services.gen.js.outputdir}/${services.gen.js.depfilename}"/>
        </code>
    </createtask>
</project>