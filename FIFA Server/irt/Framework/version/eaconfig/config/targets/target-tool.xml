<!-- 
     (c) Electronic Arts. All Rights Reserved.
-->
<project xmlns="schemas/ea/framework3.xsd">
    <!-- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -->
    <do unless="'@{OptionSetGetValue('config.targetoverrides', 'tool-build')}' == 'exclude'">
        <target name="tool-build" style="build" description="Build the tools for a specific configuration" allowoverride="true">
          <property name="eaconfig.build.group" value="tool" />
          <call target="eaconfig-build-graph" force="true" />
          <GenerateBuildLayout/>
          <nant-build configs="${config}"/>
        </target>
    </do>

    <!-- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -->
    <do unless="'@{OptionSetGetValue('config.targetoverrides', 'tool-buildall')}' == 'exclude'">
        <target name="tool-buildall" description="Build the tools for each configuration" allowoverride="true">
          <property name="eaconfig.build.group" value="tool" />
          <call target="eaconfig-build-graph-all" force="true" />
          <GenerateBuildLayout/>
          <nant-build configs="${eaconfig.build.configs}"/>
        </target>
    </do>

    <!-- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -->
    <do unless="'@{OptionSetGetValue('config.targetoverrides', 'tool-run')}' == 'exclude'">
        <target name="tool-run" description="Runs the tools for a specific configuration" depends="tool-build" allowoverride="true">
            <property name="eaconfig.build.group" value="tool" />
            <call target="eaconfig-run-caller" force="true" />
        </target>
    </do>

    <!-- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -->
    <do unless="'@{OptionSetGetValue('config.targetoverrides', 'tool-run-fast')}' == 'exclude'">
        <target name="tool-run-fast" description="Runs the tools for a specific configuration" allowoverride="true">
            <property name="eaconfig.build.group" value="tool" />
            <call target="eaconfig-run-caller" force="true" />
        </target>
    </do>

  <!-- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -->
  <do unless="'@{OptionSetGetValue('config.targetoverrides', 'tool-group-run-fast')}' == 'exclude'">
    <target name="tool-group-run-fast" description="Runs the tools for a specific configuration" allowoverride="true">
      <property name="eaconfig.build.split-by-group-names" value="false"/>
      <call target="tool-run-fast" force="true" />
    </target>
  </do>

</project>
