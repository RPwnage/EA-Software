[headername="framework/tdf/userdefines.h"]
#include "framework/gen/userdefines.tdf"

namespace Blaze
{

class UserSetRequest
{
    [tag="uset"] ObjectId mBlazeObjectId;
};

class UserSetUserIdsResponse
{
    [tag="uids", allowref=true] UserIdentificationList mUserIdList;
};

class UserSetUserBlazeIdsResponse
{
    [tag="uids", allowref=true] BlazeIdList mBlazeIdList;
};

class UserSetSessionIdsResponse
{
    [tag="sids", allowref=true] UserSessionIdList mUserSessionIdList;
};

class CountResponse
{
    [tag="cnt"] uint32_t mCount;
};

// If this seems useful somewhere else, feel free to move it out:
union RoutingOptionsId
{   
    [tag="iid"] uint16_t instanceId;            // 
    [tag="sid"] uint32_t sliverIdentity;
};
class RoutingOptionsResponse
{
    // No need to have the 'type' here since the union already handle that.
    [tag="id"] RoutingOptionsId id;
};

[description="Configuration for User Sets."]
class UserSetConfig
{
};

}
