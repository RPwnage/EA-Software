
// This isn't going to load correctly: 
gamemanager.matchmakerSettings = {
#include "component/gamemanager/matchmaker_settings.cfg"
}

gamemanager.scenariosConfig = {
#include "component/gamemanager/scenarios.cfg"
}

gamemanager.gamebrowser = {
#include "component/gamemanager/gamebrowser.cfg"
}

gamemanager.createGameTemplatesConfig = {
#include "component/gamemanager/create_game_templates.cfg"
}

gamemanager.censusData.gameAttributes =  {OSDK_gameMode = ["0", "1", "2", "3", "4", "5", "6", "8", "9"] }

// NOTE: ILT's gamesession overrides must be the same in matchmaker.cfg, gamemanager.cfg and search.cfg:
gamemanager.gamesession.gameModeAttributeName = "OSDK_gameMode"


