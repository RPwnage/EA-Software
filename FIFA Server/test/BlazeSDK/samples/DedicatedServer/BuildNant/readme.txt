These files demonstrate how to build the dedicated server with nant.

Steps:

1. Copy these files to the parent directory
2. Open a nant command prompt and type this: nant 
   To build release, type: nant -D:config=pc-vc-dev-opt

Nant will install required packages and build the dedicated server.

Note that these buildfiles are for illustrative purposes only, and are not maintained.