<project xmlns="schemas/ea/framework3.xsd">
  <property name="package.ExamplePackage.includedirs" >
    ${package.example.dir}/include
  </property>

  <fileset name="package.ExamplePackage.libs" >
    <includes name="${package.example.builddir}/${config}/lib/${lib-prefix}ExamplePackage${lib-suffix}" />
  </fileset>

  <!-- The above property and fileset definition can be replaced with -->
  <task name="ScriptInit" PackageName="ExamplePackage" />
</project>